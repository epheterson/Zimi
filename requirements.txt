libzim>=3.1.0
PyMuPDF>=1.23.0
# <2.0 on purpose: mcp 2.0 dropped the vendored FastMCP this server is built on
# (moved it to a separate `fastmcp` distribution), so a bare `mcp` resolves to
# 2.0 and the MCP server crashes on import (issue #52, hit via OpenWebUI). Pin to
# the 1.x line that still ships FastMCP until the server is ported.
mcp>=1.0,<2.0
certifi>=2024.1.1
zeroconf>=0.130.0
# Rescales a captured site's icon into the 48x48 ZIM illustration. Without it
# has_image_support() is False and every created ZIM ships iconless — which is
# exactly what happened on the Docker image, where Pillow was absent (Eric: CNN
# capture "no favicon" on prod, present locally where Pillow is installed).
Pillow>=10.0.0
# BitTorrent accelerator. Marker-gated below 3.14 (no wheel there yet, and no
# sdist) so a plain `pip install -r requirements.txt` never breaks on new
# interpreters; missing = HTTP-only. Wheels cover manylinux + musllinux, macOS
# and Windows for 3.9–3.13; Docker's base is Python 3.11 (glibc), so a wheel
# always resolves here.
libtorrent==2.0.*; python_version < "3.14"
