# Python
__pycache__/
*.pyc
*.pyo

# Environment
.env

# ZIM data
zims/
*.zim
.zimi_cache.json

# OS
.DS_Store

# Build artifacts
/build/
/dist/
/.build-venv/
*.spec
*.spec.bak
Sparkle.framework/

# Node (desktop app deps)
node_modules/
package.json
package-lock.json

# Testing & tooling
.playwright-mcp/
test-results/
tmp/
.pytest_cache/
.ruff_cache/

# Agent / scratch workspaces (never shipped)
scratchpad/
scratchpad_zims_data/
zimi-config/
.superpowers/
graphify-out/

# Root-level dev screenshots / QA artifacts (don't block images elsewhere)
/*.png
/*.jpg
/*.jpeg
/*.yml
!/docker-compose*.yml
ui-review/

# Working docs (not shipped)
PLAN.md
CLAUDE.md
.claude/

# Build/tooling artifacts. uv is not this project's package manager — the
# local pre-commit hook shells out to it, which leaves these behind.
zimi.egg-info/
uv.lock
