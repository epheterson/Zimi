services:
  zimi:
    build: .
    container_name: zimi
    restart: unless-stopped
    ports:
      - "8899:8899"
    volumes:
      - ./zims:/zims
      - ./zimi-config:/config
    environment:
      - ZIM_DIR=/zims
      - ZIMI_DATA_DIR=/config
      - ZIMI_MANAGE=1
