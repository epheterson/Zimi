services:
  zimi:
    build: .
    container_name: zimi
    restart: unless-stopped
    ports:
      - "8899:8899"
    volumes:
      - ./zims:/zims
    environment:
      - ZIM_DIR=/zims
      - ZIMI_MANAGE=1
