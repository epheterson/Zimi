services:
  zimi:
    build: .
    container_name: zimi
    restart: unless-stopped
    ports:
      - "8899:8899"
    volumes:
      - ./zims:/zims
      - ./zimi-data:/data
    environment:
      - ZIM_DIR=/zims
      - ZIMI_DATA_DIR=/data
      - ZIMI_MANAGE=1
