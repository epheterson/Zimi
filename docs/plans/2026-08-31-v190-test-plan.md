# v1.9.0 — the test plan, and the ship sequence

One day, one tag. This is what gets checked, by whom, and in what order. Part A is mine and runs unattended. Part B is yours and is the only thing standing between here and the tag — task #20, the gate you set. Part C is the ship itself, which has two manual gates that are yours by rule.

Ordered by likelihood of finding something. If a check is going to fail, it should fail early enough that we still have a day.

## The state it starts from

| | |
|---|---|
| Branch | `v1.9`, HEAD `8425585`, clean, nothing unpushed |
| Ahead of main | 222 commits |
| CI on HEAD | green (CI + Docker Build & Publish) |
| Version declared | `1.9.0` in both `pyproject.toml` and `server.py` |
| Blocking tasks | none open — #77 through #91 all closed |
| Open, deferred by decision | #13, #22, #30, #34, #40, #41, #42, #51, #56, #70, #71 (all 1.9.1) |
| Open, post-tag by your call | #4 (issue replies), #46 (GHSA advisory) |
| Open, not blocking | #92 (a flaky test, see A1) |

## Part A — machine pre-flight

No human judgement in here. Every line either produces evidence or fails.

| # | Check | Pass condition |
|---|---|---|
| A1 | `pytest tests/` | 2563+ passed, 0 failed. If #92 flakes, re-run once and capture the traceback this time — a gate that fails at random teaches people to re-run instead of read, which is how we got here |
| A2 | `./scripts/release-gate.sh` | Every feature line green. This is the one that catches a feature wired correctly in every module and still dead end to end |
| A3 | Clean-room install | `python -m build`, fresh venv, `pip install dist/*.whl`, then `zimi --version` and `zimi config`. Proves what a PyPI user gets, not what our repo does |
| A4 | `pip install zimi[mcp]` path | The MCP server starts. This is issue #52's exact path and an inbound user is standing on it |
| A5 | Paint gate on a **real** capture | Not the synthetic fixture — an actual made ZIM, opened in a browser, scrolled to the bottom, every image asked whether it decoded. This is the check I owed you and had not run |
| A6 | Prod on HEAD | `zim-reader` serving `8425585`, and a capture completes on it |

**A5 is the important one.** Every time this release has embarrassed itself, it was because something structural passed while the page looked wrong: 118 images "all resolvable" while the body rendered as attribute soup; a viewport screenshot that looked fine above a page where nothing below the fold ever loaded. `naturalWidth > 0` after a full scroll is the only honest question, and until now I had only asked it of a fixture I built myself.

## Part B — your gate (#20), about 30 minutes

Do these in order. Stop and tell me at the first one that feels wrong — don't collect a list, because a list is how we burned four rounds last week.

Everything runs against prod once A6 is green.

### What Part A already found (2026-08-31, before you touched it)

Running A5 against real targets caught two things by hand-testing what the suite could not see. Both are fixed and both have guards now.

**A whole-site crawl of sqlite.org died in 3.1 seconds.** Its homepage builds sponsor logos in JavaScript by concatenating strings that contain HTML, so the page literally holds the characters `<img src='images/…`. We were parsing `<script>` bodies as markup, read a "URL" spanning three lines of JS, and the resulting `http.client.InvalidURL` — an `HTTPException`, not an `OSError`, so `except OSError` missed it — ended the whole job. Both halves fixed: script/style/comment bodies are now masked out of every scan, and no single unusable asset reference can end a job.

**72 of cnn.com's 117 images never painted**, which is your "only the first n images then nothing", measured at last. Every one of the 72 was `loading="lazy"`, 59 of them had a zero-sized box, and there were **zero HTTP failures** — the browser never asked. CNN's homepage is a JavaScript application; without its JS the rails collapse to zero height, and a lazy image in a zero-height container never approaches the viewport, so scrolling can never rescue it. Lazy loading is a bandwidth optimisation for the live web and is pure loss offline, so captured pages now load eagerly.

**Set expectations on CNN specifically:** the Fast engine does not run JavaScript, so a JS-driven *homepage* captures as a shell — 6.8 KB of text, against 63.8 KB for a Wikipedia article. Every image we carried should now paint, but the page will still be much thinner than the live one. That is what the Alive engine is for, and it is the honest answer rather than a bug.

### B1 — one page, Fast (2 min)

**+ → Web page → `cnn.com` → Fast.** Watch the run: counters climb, they don't run backwards, and the packaging phase says it's still working rather than sitting mute.

Then **open it and scroll the whole thing.** Pass: the article body is prose, not `data-fave-thumbnails="{"big":...`. Images paint all the way down, not just the first screen. The favicon in the tab is CNN's. One size on the completion card, and the WHAT'S INSIDE bar underneath agrees with it exactly.

*Why this one:* it's the page that broke in three different ways — attribute soup (#87), evicted response bodies, and the srcset variant ceiling. It's the hardest single page we capture.

### B2 — a real site, start it and walk away (10 min in the background)

**+ → Whole site → `https://www.sqlite.org/` → max pages 25.** Start it, then go do B3–B6 while it runs.

*Why this target and not apple.com:* apple.com's homepage is a JS-heavy marketing page behind a CDN that throttles headless clients — it's the pathological case that wedged for 80 minutes and is now fixed, but it is not what a person means by "capture a site". sqlite.org is static HTML with real images, real internal links, and a real structure. If whole-site capture is good, it is good here first.

Come back to it: it finished, it's in your library, internal links between captured pages work, and clicking a link we *didn't* capture shows a real page explaining that — not raw JSON.

### B3 — does the library still feel fast (3 min)

You said it felt slower than it used to. Home page paint, search, open Wikipedia, hit back. Then **About this ZIM on Wikipedia specifically** — the composition bar there is computed off the request path now, but 124 GB is the input that matters and I have only measured it, not felt it.

Pass is your judgement, not a number. If it drags, that's a hold.

### B4 — the numbers agree (1 min)

Manage → any ZIM. Its size in the list, in About this ZIM, and on the completion card of anything you just made are the same number. Wikipedia should read **124 GB**, matching what the NAS says, not 115.

*Why:* all sizes are decimal now (÷1000, like Kiwix and `ls`), and no size derived from bytes is cached anywhere. 0 of 67 entries disagreed when I checked; you should see zero too.

### B5 — recents tell the truth (1 min)

Delete one of the ZIMs you just made, from Manage. Go back to **+**. That row should already say it's gone — without you clicking it. Every other row still accurate.

### B6 — the four held issues (5 min)

These have replies drafted and waiting on the tag (#4). Confirm each before I post anything:

- **#48** — Almanac → time machine → 2100. Planets move, sliders dim, readout says 2100. Return to now: sliders wake.
- **#49** — right-click the logo, a source tile, a search result. "Open link in new tab" exists and works.
- **#50** — Catalog → search `mdwiki`. One card, two flavors, one checkmark, and the 10.75 GB video build is not preselected.
- **#51** — the crash-on-download fix. Measured, not clickable: worst lock wait 10.6s → 0.45s, archives opened 53 → 1.

### B7 — the upgrade path (2 min)

Manage → Server → App updates. It knows this is Docker and hands you a `docker compose pull`. That's the sentence every existing user reads on release day, so it should be the right one.

## Part C — ship sequence

Two gates in here are yours by rule and I will not move past either without you saying so.

1. **I** set the CHANGELOG date to 2026-08-31 (it currently reads 2026-08-20) and push.
2. **I** open the PR from `v1.9` → `main`, with a body you read before it exists publicly.
3. **You** merge it. *(Gate 1.)*
4. `auto-release.yml` sees `version = "1.9.0"` change on main, tags `v1.9.0`, and creates **one** draft release with notes from CHANGELOG.md.
5. `desktop-release.yml` fires on the tag, builds every platform, signs the Sparkle enclosures, refreshes the appcasts, and attaches everything to that same draft.
6. **You** review the draft and publish it. *(Gate 2.)*
7. `publish-release.yml` ships PyPI and snap. Docker `latest` publishes off main.
8. **I** verify every channel actually serves 1.9.0 — PyPI, Docker Hub, snap, brew cask, Sparkle appcast — and report what I saw, not what should have happened.
9. Then, and only on your word: #4 (post the four issue replies) and #46 (the GHSA advisory).

## Part D — what is deliberately not in 1.9

So nothing here is a surprise on release day, and so no one reads a promise we didn't keep:

- **Editing a ZIM** (#13) — engine exists on a branch, lands in 1.9.1.
- **Resource-aware offers** (#42) — we removed the "wants ~1 GB free memory" line rather than ship a claim nothing measures. Real capability detection is 1.9.1.
- **Capture budget economics** (#40), **stall salvage and browser recycling** (#41) — the create engines work; they are not yet frugal.
- **External zimit** (#70) and **external qBittorrent** (#71) — both asked for, both 1.9.1.
- **CSV user import** (#56).
- **Blocked-provenance in alive ZIMs** (#30) — warc2zim writes that file, so we can't stamp it yet.
- **Wikipedia footer sister-project logos** (#51 in the create list) — a UA-specific carry miss, cosmetic.

## The thing worth remembering from this release

Nearly every bug that cost us a day was the same shape: **something reported success, or a bound, without checking it.** A pipe that returned `tail`'s exit code. A test runner that exits 0 on a usage error. A release gate that imported a module and called it a test run. A ceiling a subcontractor walked past. A sweep with limits that never said where it was inside them, which fooled our own watchdog before it fooled anyone else.

It happened again while writing this plan: `pytest --timeout=900` with pytest-timeout not installed ran **zero tests** and the harness reported exit code 0.

Fixing the instrument is what found the bugs. That is the habit to keep.
