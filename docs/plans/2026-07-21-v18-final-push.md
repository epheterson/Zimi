# v1.8 final push — Eric's 10:24 PM list (2026-07-21)

Every item, its lane. Nothing falls off.

## 1.8 NOW — quick fixes (Wave 1)
- W1.1 Almanac: hide downloads badge when almanac open (was showing on the X)
- W1.2 Keychain: password form semantics so iCloud offers save immediately
  (proper form submit + autocomplete attrs), and FIX "remember me" (broken)
- W1.3 Manage>Library: amber-different "Reorder" pill at end of category pill
  row → opens reorder panel; drag-drop reordering of those pills in place
- W1.4 Server settings: prefetch all settings on entering manage view; view
  renders prefilled+current (fix slow + initially-incorrect)
- W1.5 Hot zims: add Hide next to Save (has show, no hide)
- W1.6 Caches: title the section; horizontal bar chart of data-dir usage
  breakdown w/ top-ZIM contributions highlighted
- W1.7 BT settings textboxes narrower (single digits; ratio allows 1.5)
- W1.8 Reader topbar icon order: pop-out ALWAYS farthest right, random
  farthest left; judgment on what else goes behind …
- W1.9 openstreetmap-wiki BT slow: diagnose on NAS + add "switch to direct
  download" action on in-flight BT downloads (+ peer tricks if cheap)

## 1.8 — bigger (Wave 2)
- W2.1 Reader View round 2: kill top dead space; settings palette (font
  serif/sans, light/dark, size) + AUTO reader mode (articles open directly
  in Reader View, dark unbroken). Print/PDF-export/share: design where they
  live (reader palette vs … menu) — propose, Eric approves.
- W2.2 WinSparkle: auto-update for the new Windows build (mac has Sparkle;
  wire WinSparkle or equivalent appcast flow into the portable/installer)
- W2.3 Repo grow-up audit: privacy rock-solid (no secrets/personal anywhere,
  history too), root decluttered, md files minimal/professional, structure
  like "amazing projects not slop" — audit + fixes
- W2.4 Full i18n audit: every word, every view, all 10 languages — hunt
  untranslated strings, bad tone, truncation
- W2.5 a11y re-audit of all new 1.8 surfaces (keep the 100/100 honest)

## 1.9 — confirmed by Eric tonight
- ZIM creation v1 (libzim writer: bookmarks/notes/page → ZIM) then zimit v2
- Almanac→ZIM deep-links (hero) + almanac-as-ZIM once creation lands
- Delta updates; partial downloads as size-variants of ONE catalog entry
  (not the ecosystem's N-entries mess)
- Users/profiles: per-user ZIM allowlists (yes per Eric), kids/schools modes,
  history monitoring = OPEN DESIGN QUESTION (what settings worldwide?)
- Word lookup popover (wiktionary) — "wow"
- Search: beautiful results, phrase queries, highlights; discover↔search
  surprise-and-delight tie-ins
- Reading resume = history w/ stored position; highlights row below discover
- Related articles (keyword); random-by-topic discover card w/ dice refresh;
  random video discovery
- Maps done right: integrated experience, article-location → map, discover
  integration (design doc first)
- Library health report; video-ZIM + zimgit/PDF first-class polish pass
- Scheduled/bandwidth-windowed downloads ("download at night")
- Backup/import/export hub incl. library-list import next to Import ZIM
- New ZIM format: backwards-compatible extras bundle

## Decisions Eric owes (flagged, not blocking wave 1)
- D1 username+password (vs password-only) — in 1.8 auth fix or with 1.9 users?
- D2 AUTO reader mode default behavior (per-ZIM? global toggle?)
- D3 selection-bar button unification (from design audit — still open)
- D4 demo GIF approval (in PR)
